services:

  backend:
    build:
      context: ./desafio-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./desafio-frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend